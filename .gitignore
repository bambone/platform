# Laravel
*.log
.env
.env.backup
.env.production
.env.local
.env.testing
.env.*.local
/auth.json
/node_modules
/public/build
/public/hot
/public/images
# Собирается из resources/themes + theme:sync-platform-assets; дублирует legacy — не хранить в git
/public/themes
# Hero-ролик с прода (~48MB); локально скачать с motolevins.rentbase.su или держать только в R2 у тенанта
/resources/themes/moto/public/videos/*.mp4
/public/storage
# Не создавать: перекрывает маршрут Filament «Клиенты» (/tenants) на platform host; брендинг — storage/app/public/tenants → /storage/…
/public/tenants
/storage/*.key
/storage/pail
/vendor
/database/database.sqlite
/database/database.sqlite-journal
/database/scripts

# Laravel compiled/cache (backup if nested .gitignore missing)
/storage/framework/cache/data/*
/storage/framework/sessions/*
/storage/framework/views/*.php

# Composer
composer.phar

# PHP
.phpactor.json
.phpunit.result.cache
.phpunit.cache
*.phpunit.result.cache
.php-cs-fixer.cache
/coverage

# IDE & Editors
/.claude
/.cursor
/.fleet
/.idea
/.nova
/.vscode
/.zed
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# OSPanel (локальная конфигурация)
/.osp

# Laravel / Dev
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
yarn-debug.log
.npm
.yarn-integrity


docs/tenants_tz/*

# Дампы «все изменённые файлы после пуша» (scripts/export-changed-files-since-push.ps1)
/scripts/export-changed-files-dumps/

